import { UserIcon } from "@heroicons/react/24/solid";
import Image from "next/image";
import Link from "next/link";
import { ReactNode } from "react";

export default function AlugadosRecentemente({ children }: { children: ReactNode }) {
  return (
     <div>
        {children}
    </div>
  );
}